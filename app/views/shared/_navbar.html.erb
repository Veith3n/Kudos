<nav class='navbar navbar-default navbar-static-top'>
  <div class='container-fluid'>
    <div class='navbar-header'>
      <a class='navbar-brand'>Kudos App</a>
    </div>

    <div class='collapse navbar-collapse'>
      <ul class='nav navbar-nav'>
        <li><%= link_to 'Home', root_path, :class => current_class?(root_path) %></li>
        <li><%= link_to 'Teams', teams_path, :class => current_class?(teams_path) %> </li>
        <li><%= link_to 'Users', users_path, :class => current_class?(users_path) %> </li>
      </ul>

      <ul class='nav navbar-nav navbar-right'>
        <div class='collapse navbar-collapse'>
          <form class='navbar-form navbar-right'>
            <% if user_signed_in? %>
              <%= link_to 'Edit your data', profile_users_path(current_user), class: 'btn btn-link  float: right' %>
              <% if current_user.provider == nil %>
                <%= link_to 'Change your password', edit_user_registration_path %>
              <% end %>
              <%= link_to 'Create team', new_team_path, class: 'btn btn-primary  float: right' %>
            <% end %>
            <% unless current_page?(root_path) %>
              <% if user_signed_in? %>
                <%= link_to 'Logout', destroy_user_session_path, class: 'btn btn-info  float: right', method: :delete %>
              <% else %>
                <% unless current_page?(new_user_session_path) %>
                  <%= link_to 'Login', new_user_session_path, class: 'btn btn-primary' %>
                <% end %>
                <% unless current_page?(new_user_registration_path) %>
                  <%= link_to 'Register', new_user_registration_path, class: 'btn btn-info' %>
                <% end %>
              <% end %>
            <% end %>
          </form>
        </div>
      </ul>
    </div>
  </div>
</nav>
